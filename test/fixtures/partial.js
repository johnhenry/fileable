// Stands in for a pre-compiled `.jsx` partial (see the src-resolution note in
// src/resolve.ts) -- a plain descriptor object, same shape jsx-runtime.js
// would have produced.
export default { tag: "file", props: {}, children: ["partial content"] };
